using Miru.Config;

namespace Miru.Urls
{
    public static class UrlMiruConfigExtensions
    {
        public static QueryStringConfig UseDefaultMiruConfig(this QueryStringConfig config)
        {
            
            
            return config;
        }
    }
}