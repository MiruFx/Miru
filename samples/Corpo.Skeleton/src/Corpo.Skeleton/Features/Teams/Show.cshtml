@model TeamShow.Result

<div class="row justify-content-center">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">

                <h3>Team</h3>
                
                <a for="@(new TeamList())">← Teams</a> | 
                <a for="@(new TeamEdit.Query { Id = Model.Team.Id })">Edit</a>
                
                <dl class="dl-horizontal mt-4" miru-id="@Model">
                    <dt>
                        <mdl for="Team.Name" />
                    </dt>
                    <dd>
                        <md for="Team.Name"/>
                    </dd>
                </dl>

            </div>
        </div>
    </div>
</div>