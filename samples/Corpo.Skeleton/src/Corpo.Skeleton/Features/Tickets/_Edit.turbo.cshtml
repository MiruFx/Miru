@model TicketEdit.Result

<turbo-stream action="replace" target="@Naming.Form(Model)">
    <template>
        <div class="alert alert-success my-4">
            Ticket successfully saved
        </div>
    </template>
</turbo-stream>