using Miru.PageTesting;
using NUnit.Framework;

namespace Corpo.Skeleton.PageTests.Pages.Tickets
{
    public class TicketShowPageTest : PageTest
    {
        [Test]
        public void Can_show_ticket()
        {
            // var ticket = _.MakeSaving<Ticket>();
            
            // _.Visit(new TicketShow.Query { Id = ticket.Id });

            // _.ShouldHaveText(ticket.Name);
        }
    }
}